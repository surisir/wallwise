# Browser API traffic is same-origin via /api/backend; do not expose FastAPI here.
# Set this only on the server/build host, for example in Vercel:
# BACKEND_URL=https://your-backend.up.railway.app
BACKEND_URL=http://127.0.0.1:4000
